# Environment Configuration
NODE_ENV=production
PORT=3367

# Security Configuration
# In production, set this to your actual domain
# ALLOWED_ORIGINS=https://yourdomain.com

# Content limits
# MAX_SECRET_LENGTH=50000

# Password Generator Configuration
# PASSWORD_EXCLUDE_CHARS=0O1l
# PASSWORD_SPECIAL_CHARS=#_-

# Rate limiting (optional overrides)
# CREATE_RATE_LIMIT=10
# RETRIEVE_RATE_LIMIT=50
# RATE_LIMIT_WINDOW_MS=900000

# Storage Configuration
# Priority order: Upstash Redis > Vercel KV > Memory

# Upstash Redis (Recommended for your setup)
# UPSTASH_REDIS_REST_URL=your_upstash_redis_url
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# Alternative: Direct Redis URL (your current setup)
# REDIS_URL=redis://username:password@host:port

# Vercel KV (Alternative option)
# KV_REST_API_URL=your_vercel_kv_url
# KV_REST_API_TOKEN=your_vercel_kv_token

# Storage Backend (auto-detected, but can be forced)
# STORAGE_BACKEND=upstash-redis|redis-url|vercel-kv|vercel-runtime|memory